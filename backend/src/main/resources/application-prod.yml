# Production profile (Render + Neon Postgres). Activated with SPRING_PROFILES_ACTIVE=prod.
# All values come from environment variables set in the Render dashboard.
spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      # Small pool for Neon's free connection budget. Recycle connections well
      # before Neon drops idle ones; keepalive holds the minimum idle open.
      # For best results point DATABASE_URL at Neon's POOLED (pgBouncer) endpoint.
      maximum-pool-size: ${DB_POOL_SIZE:5}
      minimum-idle: 1
      connection-timeout: 20000
      keepalive-time: 150000
      max-lifetime: 300000
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true

app:
  # The deployed frontend. Overridden by FRONTEND_URL like everything else here;
  # the fallback is spelled out because the default profile's is localhost, and
  # a production instance that quietly emailed creators a localhost dashboard
  # link would be worse than one that never started.
  frontend-url: ${FRONTEND_URL:https://localmediakit.vercel.app}
